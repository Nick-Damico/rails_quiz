<li class="flex items-center border border-white p-2">
  <%= card.front %>
  <div class="inline-flex text-xs border border-gray-400 ml-auto">
    <%= link_to 'view', card_path(card), class: 'bg-primary text-white px-2 py-2' %>
    <%#= link_to 'delete', card_path(card),
      data: {
        turbo_confirm: t("buttons.cards.delete"),
        turbo_method: :delete
      },
      class: 'bg-alert text-white px-2 py-2' %>
  </div>
</li>