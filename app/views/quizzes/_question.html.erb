<li class="flex items-center border border-white p-2">
  <%= question.content %>

  <div class="inline-flex text-xs border border-gray-400 ml-auto">
    <%= link_to 'view', quiz_question_path(@quiz, question), class: 'bg-primary text-white px-2 py-2' %>
    <%= link_to 'destroy', quiz_question_path(@quiz, question),
      data: {
        turbo_confirm: t("buttons.questions.delete"),
        turbo_method: :delete
      },
      class: 'bg-alert text-white px-2 py-2' %>
  </div>
</li>