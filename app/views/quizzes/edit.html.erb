<div class="border-b border-gray-800 mb-6">
  <%= back_button path: author_quiz_path(@author, @quiz) %>
</div>

<h2 class="text-2xl text-center font-semibold mb-4">Edit Quiz</h2>

<%= render 'form', quiz: @quiz, author: @author, url: author_quiz_path(@author, @quiz) %>
