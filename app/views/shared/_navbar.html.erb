<nav class="bg-white  dark:bg-gray-900 border-b border-gray-700" data-controller='navbar'>
  <div class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto">
    <a href="<%= root_path %>" class="flex items-center p-4 space-x-3 rtl:space-x-reverse">
        <img src="https://flowbite.com/docs/images/logo.svg" class="h-8" alt="Flowbite Logo" />
        <span class="self-center text-2xl font-semibold whitespace-nowrap dark:text-white">Quiz<span>IT</span></span>
    </a>

    <button
      data-navbar-target='button'
      data-collapse-toggle="navbar-default"
      type="button"
      data-action='click->navbar#onClick'
      class="inline-flex items-center p-2 w-10 h-10 justify-center text-sm text-gray-500 rounded-lg md:hidden hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-gray-200 dark:text-gray-400 dark:hover:bg-gray-700 dark:focus:ring-gray-600"
      aria-controls="navbar-default"
      aria-expanded="false"
    >
        <span class="sr-only">Open main menu</span>
        <svg class="w-5 h-5" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 17 14">
            <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 1h15M1 7h15M1 13h15"/>
        </svg>
    </button>

    <div class="hidden w-full md:block md:w-auto" id="navbar-default" data-navbar-target='linksContainer'>
      <ul class="font-medium flex flex-col mt-4 border rounded-lg md:flex-row rtl:space-x-reverse md:mt-0 md:border-0 bg-gray-900 border-gray-700">
        <li>
          <a href="<%= root_path %>" class="flex py-4 px-4 text-white hover:text-green-500" aria-current="page">Home</a>
        </li>

        <% if user_signed_in? %>
          <li class="relative group">
            <button class="flex items-center py-4 px-4">
              <span class="mr-1">Design</span>
              <%= render "icons/chevron_down", klass: "group-hover:hidden size-5" %>
              <%= render "icons/chevron_up", klass: "hidden group-hover:block size-5" %>
            </button>

            <%# BEGIN Design sub-navigation %>
            <ul class="mx-4 md:ml-4 hidden group-hover:block md:absolute md:bg-gray-900 border border-gray-700 md:p-3 md:z-50 md:rounded md:top-12">
              <li>
                <a
                  href="<%= new_author_quiz_path %>"
                  class="flex items-center py-4 px-4 text-gray-900 rounded hover:bg-gray-800 md:border-0 dark:text-white hover:text-green-500 "
                >
                  <%= render "icons/clipboard_doc", klass: "size-5 mr-1" %> Quizzes
                </a>
              </li>
              <li>
                <a
                  href="<%= new_author_quiz_path %>"
                  class="flex items-center py-4 px-4 text-gray-900 rounded hover:bg-gray-800 md:border-0  dark:text-white hover:text-green-500 "
                >
                  <%= render "icons/flash", klass: "size-5 mr-1" %> Flashcards
                </a>
              </li>
            </ul>
          </li>
          <%# END Design sub-navigation %>

          <li>
            <a href="<%= author_quizzes_path() %>" class="block py-4 px-4 rounded hover:bg-gray-100 md:hover:bg-transparent md:border-0 text-white hover:text-green-500">Study</a>
          </li>
        <% end %>

        <li>
          <% if user_signed_in? %>
            <%= link_to "Sign out",
                destroy_user_session_path,
                data: { turbo_method: :delete, turbo_confirm: "Are you sure?" },
                class: "block py-4 px-4 text-gray-900 rounded hover:bg-gray-100 md:hover:bg-transparent md:border-0 md:hover:text-blue-700 dark:text-white md:dark:hover:text-green-500 dark:hover:bg-gray-700 dark:hover:text-white md:dark:hover:bg-transparent" %>

          <% else %>
            <a href="<%= new_user_session_path %>" class="block py-4 px-4 rounded hover:bg-gray-100 md:hover:bg-transparent md:border-0 text-white hover:text-green-500">Sign In</a>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>
