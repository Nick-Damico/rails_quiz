<div class="flex text-sm space-bet my-1 items-center">
  <p class="uppercase mr-3">Created by:</p>
  <%= link_to dashboard_user_path(object.author), class: "flex items-center" do %>
    <%= avatar(object.author) %>
    <p class="italic font-light ml-3 max-w-prose"><%= object.author.username %></p>
  <% end %>
</div>